<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <property name="DATETIME" value="%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX}"/>
    <property name="PATTERN" value="[${DATETIME}] %-5p %c - %m%n"/>

    <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <Pattern>${PATTERN}</Pattern>
        </encoder>
    </appender>

    <appender name="RollingFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/discord/current.log</file>

        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- Daily rollover or when over 10MB -->
            <fileNamePattern>logs/discord/%d.log.%i.gz</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>

            <!-- Keep props -->
            <totalSizeCap>20GB</totalSizeCap>
            <maxHistory>60</maxHistory>
        </rollingPolicy>

        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <Pattern>${PATTERN}</Pattern>
        </encoder>
    </appender>

    <root level="trace">
        <appender-ref ref="Console"/>
        <appender-ref ref="RollingFile"/>
    </root>

</configuration>